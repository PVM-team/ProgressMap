describe('StateService', function() {
 var scope;

  beforeEach(function() {
     module('ProgressApp'); 

     inject(function($rootScope) {
        scope = $rootScope.$new();
     
     });
  });

  it('should do something', function() {
      
  });
});
